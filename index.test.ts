import { IrisSoftwareGroupCascade } from "./index";

describe("iris-software-group-cascade-typescript-sdk", () => {
    it("initialize client", async () => {
        const irissoftwaregroupcascade = new IrisSoftwareGroupCascade({
        });
    });
});
